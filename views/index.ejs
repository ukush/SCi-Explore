<!--The map will go here-->

<div id ="map"></div>
<div id="coordinates">

</div>
<script>
    var data = <%-JSON.stringify(data)%>;
    var allcoords = []
    console.dir(data[0][0][1])
    for (var i=0;data[0].length;i++) {
        try {
            if (i == 118) {
                break;
            }
        if(data[0][i][1] != null){
            let mid = data[0][i][1].coordinates[0]
            //console.dir(mid)
            //console.dir(i)
            allcoords.push(mid)
        }
    } catch (error) {
        console.dir(error)
    }
    }
    
    console.dir(allcoords)
</script>
<script>
    var map = L.map('map').setView([53, -4], 6);

var addressPoints = allcoords
//var temp = JSON.stringify(addressPoints)
//console.dir(addressPoints.length)

var polygon = L.polygon([
    [51.509, -3.08],
    [51.51, -0.047],
    [55.503, -0.06],
    [55.503, -3.06]
])

L.tileLayer('').addTo(map);

var polygons = L.layerGroup([polygon]);

var ctrl = L.control.layers([]);

/*
const geojsonMarkerOptions = {
    radius: 8,
    fillColor: "#ff7800",
    color: "#000",
    weight: 1,
    opacity: 1,
    fillOpacity: 0.8,
  };

var lightData = L.geoJSON(addressPoints, {
    onEachFeature: function(feature, layer) {
        layer.bindPopup("<h1>"+feature.type+"</h1>")
        latlng()
    },
    pointToLayer: function (feature, latlng) {
        console.dir(latlng)
        console.dir(1)
      return L.circleMarker(new L.latlng(feature.coordinates[0], feature.coordinates[1]), geojsonMarkerOptions);
    },
  });
*/
var markers = L.markerClusterGroup();



ctrl.addOverlay(markers, 'Pins');
ctrl.addOverlay(polygons, 'Polygons');
ctrl.addTo(map);


    for (var i = 0; i < allcoords.length; i++) {
        try {
            for(var j=0; j< allcoords[i].length;j++){
                console.dir(allcoords[i][0][j])
                var marker = new L.marker(new L.LatLng(allcoords[i][0][j], allcoords[i][0][j]));
            //marker.bindPopup(title);
            markers.addLayer(marker);
            
            }
            map.addLayer(markers);
        } catch(error) {
            console.dir(error)
        }
}

L.layerGroup(L.latlng)

// add map scale
L.control.scale({ position: 'bottomleft' }).addTo(map)


// add coordinate mouse position
map.on('mousemove', function(e) {
    var latlong = `+${e.latlng.lat.toFixed(10)} ${e.latlng.lng.toFixed(10)}`
    $('#coordinates').html(latlong)
});

// add sci logo onto map as overlay
L.Control.Watermark = L.Control.extend({
    onAdd: function(map) {
        var img = L.DomUtil.create('img')
        img.src = 'images/sci-logo.png'
        img.style.width = '60px'
        return img
    },
    onRemove: function(map) {}
});

L.control.watermark = function(openstreetmap) {
    return new L.Control.Watermark(openstreetmap)
}

L.control.watermark({ position: 'bottomright' }).addTo(map)
L.tileLayer('https://{s}.basemaps.cartocdn.com/dark_all/{z}/{x}/{y}{r}.png', {
    attribution: '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors &copy; <a href="https://carto.com/attributions">CARTO</a>',
    subdomains: 'abcd',
    maxZoom: 20
}).addTo(map)
</script>
<script src="../api.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>